import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const Color primary = Color(0xFF2E7D32);
  static const Color secondary = Color(0xFF00ACC1);
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color cardDark = Color(0xFF212121);

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFAAAAAA);

  // Indicator colors
  static const Color expense = Color(0xFFE57373);
  static const Color income = Color(0xFF81C784);

  // Account colors
  static const Color bank = Color(0xFF81C784);
  static const Color cash = Color(0xFF4DD0E1);

  // Chart colors
  static const Color chartLine = Color(0xFF81C784);
  static const Color chartGrid = Color(0xFF2A2A2A);

  // Budget colors
  static const Color budgetBackground = Color(0xFF2E7D32);
  static const Color budgetProgress = Color(0xFF388E3C);

  // Misc
  static const Color divider = Color(0xFF303030);
  static const Color cardSelected = Color(0xFF2E7D32);
  static const Color cardUnselected = Color(0xFF424242);
}
